%h2 Send invitation

= semantic_form_for @user, :url => send_invitation_admin_users_path do |f|
  = f.inputs do
    = f.input :email, :required => true
  = f.actions do
    = f.action :submit, :as => :button, :label => "Send an Invitation"
